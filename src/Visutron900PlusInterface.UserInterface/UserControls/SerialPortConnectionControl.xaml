<UserControl x:Class="Visutron900PlusInterface.UserInterface.UserControls.SerialPortConnectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="100" Width="100">
    <Grid Background="LightGray">
        <StackPanel Orientation="Vertical">
            <Button Content="Create" Margin="5" Command="{Binding CreateCommand}"/>
            <Button Content="Open" Margin="5" Command="{Binding OpenCommand}"/>
            <Button Content="Close" Margin="5" Command="{Binding CloseCommand}"/>
        </StackPanel> 
    </Grid>
</UserControl>
